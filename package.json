{
  "name": "experiments-base",
  "version": "0.0.1",
  "scripts": {
    "start": "npx browser-sync start --server './' --files '**/*' --port 4000"
  },
  "devDependencies": {
    "browser-sync": "^2.26.7"
  }
}
